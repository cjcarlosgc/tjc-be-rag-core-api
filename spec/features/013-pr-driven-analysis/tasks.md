# 013 — Tareas

## Baseline T-001

- [x] Consolidar SYSTEM-2.2 e INTEROP-2.2 y sincronizar mirrors.
- [x] Re-baselinar backlog y registrar KEEP/ADAPT/DEFER/DROP.
- [x] Homologar SDD 2.0, constituciones, state y CHANGELOG en los tres repositorios.
- [x] Verificar los 15 casos, decisiones pendientes y ausencia de contradicciones legacy.
- [x] Registrar evidencia de T-001 sin iniciar implementación posterior.

## Implementación posterior — requiere selección/aprobación humana

- [x] HU30: repository binding user-centric: discovery OAuth efímero, validación de GitHub App por repositorio, ramas por installation token y `integrationBranch` obligatoria sin default. Smoke test end-to-end contra Render+Supabase+GitHub reales confirmado para verify-app-access/branches/create binding; discovery (`GET /integrations/github/repositories`) sigue sin probarse porque requiere login GitHub OAuth real (provider token) — ver `harness/state.json`.
- [x] HU31-HU32: dominio AnalysisRun, webhook y lifecycle PR/HEAD sobre bindings habilitados (firma HMAC, idempotencia por delivery, normalización `pull_request` opened/synchronize/edited/converted_to_draft/closed, chequeo de `integrationBranch`, `startRun`/`closeRun` — ver `github-webhooks.service.ts`).
- [x] HU31: revocación de instalación — `installation` (`deleted`->`REVOKED`, `suspend`->`DISABLED`, `unsuspend`->`ENABLED` para todos los bindings de esa instalación) e `installation_repositories` (`removed`->`REVOKED` solo el binding de ese repo). `ENABLED` nunca reactiva un binding ya `REVOKED`. Simplificación documentada: `unsuspend` siempre reactiva a `ENABLED` sin distinguir si el `DISABLED` vino de un `suspend` de GitHub o de una desconexión manual del usuario (mismo status, sin flag para diferenciarlos todavía).
- [x] HU33-HU34: CHANGESET, INDEX DELTA, símbolos cambiados/impactados (reindexado completo del árbol en cada Run vía GitHub API real; sin persistencia incremental optimizada todavía — ver `snapshot-analysis-job.handler.ts`).
- [x] HU35-HU36: Functional Knowledge versionada (ACTIVE/SUPERSEDED), disparo de ACTION_REQUIRED por símbolo DIRECTLY_CHANGED sin cobertura, preguntas adaptativas una a la vez, conflicto HU51 (§6.11) y continuation job. Simplificaciones documentadas: sin normalización semántica de `normalizedRule` vía LLM, sin `visualAid`, conflicto por match exacto de scope+símbolo (sin jerarquía PROJECT⊃MODULE⊃CLASS) — ver `harness/state.json`.
- [x] Validation (plan.md corte #6, sin HU propia — soporta HU32/HU39/HU40): genera y valida pruebas (arm RAG) para símbolos DIRECTLY_CHANGED METHOD/FUNCTION cubiertos por Functional Knowledge y sin test existente; corre en Sandbox, clasifica el Run (SUCCESS/BEHAVIORAL_MISMATCH/TECHNICAL_GENERATION_FAILURE/NO_ADDITIONAL_TESTS_REQUIRED) y persiste `GeneratedTestProposal`. Cierra el hueco "Run queda en PROCESSING" de HU33-36. Simplificaciones documentadas: sin baseline real (`phase=BASELINE` del contrato Sandbox 2.0 no existe en `SandboxExecutionService`, requiere migración coordinada con el repo del Sandbox — un símbolo con test existente se salta sin confirmar que siga en verde, sin `BASELINE_FAILED` todavía); `TEST_ASSERTION` se clasifica como BEHAVIORAL_MISMATCH y `COMPILATION`/`TEST_RUNTIME`/config como TECHNICAL_GENERATION_FAILURE sin juicio semántico vía LLM; solo arm RAG (GENERALIST_AGENT queda reservado para HU19); símbolos en serie — ver `harness/state.json`.
- [x] HU39: Checks nativos por HEAD. Nuevo módulo `checks/` (`AnalysisRunChecksService`) publica un GitHub Check `completed` sobre `run.headSha` cada vez que un Run termina (los 3 job handlers que completan Runs lo llaman tras la transición): `SUCCESS`/`NO_ADDITIONAL_TESTS_REQUIRED`/`NO_TEST_RELEVANT_CHANGES`->`success`, `BEHAVIORAL_MISMATCH`/`TECHNICAL_GENERATION_FAILURE`->`failure`, `BASELINE_FAILED`/`INFRASTRUCTURE_FAILURE`->`neutral` (no le achaca el problema al PR), `ACTION_REQUIRED`->`action_required` (conclusión nativa de GitHub para "hace falta que el usuario actúe"). `details_url` apunta a Console vía `CONSOLE_BASE_URL` (opcional, se omite si no está configurada). Best-effort a propósito: un fallo publicando el Check nunca revierte el Run ya completado en Core, solo se loguea. Requiere que la GitHub App tenga el permiso `checks: write` habilitado en su configuración real de GitHub (ya aprobado en `system-contract.md`, falta confirmar que esté marcado en la App real).
- [x] HU40: review, freshness y companion PR. Nuevo módulo `publications/`: `POST /analysis-runs/{id}/test-publications` valida Run `SUCCESS`+vigente y que las `proposalIds` sean `AVAILABLE` de ese Run, crea `TestPublication` `PENDING` y encola `TestPublicationJobHandler`. El job vuelve a comprobar freshness contra el HEAD real del PR (`GithubRepositoryContentService.getPullRequestHead`) antes de tocar GitHub -si cambió, `STALE`, sin escribir nada-; crea (o reutiliza, en un reintento) la rama `rag-tests/pr-<number>-<shortSha>` desde `run.headSha` vía Git Data API (`github-git-data.service.ts`: blob->tree->commit->ref), y abre el companion PR hacia `run.headRef` (la feature branch, nunca `integrationBranch`). Nunca reabre un PR cerrado con ese head branch (`FAILED` explícito si lo encuentra así); reutiliza uno abierto si ya existe. `GET /test-publications/{id}` expone el estado. Nuevos códigos de error (el contrato no los listaba explícitamente): `TEST_PUBLICATION_NOT_FOUND`, `TEST_PUBLICATION_INVALID_RUN_STATUS`, `TEST_PUBLICATION_PROPOSAL_NOT_AVAILABLE`. Simplificación documentada: el freshness check solo marca `STALE` la `TestPublication`, no hace un barrido retroactivo marcando `STALE` cualquier `GeneratedTestProposal` `AVAILABLE` de Runs ya obsoletos.
- [ ] HU41-HU42: adapters PHP/Laravel/PHPUnit en Core.
- [ ] HU57: binding lifecycle en `repository-bindings/` — `409 REPOSITORY_ALREADY_BOUND` (orden: `PROJECT_NOT_FOUND`, `BINDING_ALREADY_EXISTS`, `GITHUB_APP_ACCESS_REQUIRED`, `REPOSITORY_ALREADY_BOUND`, `INTEGRATION_BRANCH_NOT_FOUND`; `repositoryId` resuelto contra GitHub; `P2002` -> `409`), ruta `POST .../integrations/github/enable` (idempotente, revalida App y refresca `installationId`, reactiva `REVOKED` si hay acceso, `403` si no), `disable` sin degradar `REVOKED` y `installation.unsuspend` sin reactivar bindings pausados por el usuario. Pruebas incluidas.
- [ ] HU56: `DELETE /projects/{projectId}` (`204`) — migración `projects.deletedAt`; transacción única (marcar, borrar `RepositoryBinding`, cancelar/obsoletar Runs y jobs); `deletedAt IS NULL` en projects, repository-bindings, analysis-runs (incl. `GET /analysis-runs`), functional-knowledge, functional-questions, test-publications, project-versions, test-targets y experiment-runs; validación `binding.projectId == run.projectId` y Project vivo en los job handlers de checks, publications, validation y snapshot-intelligence. Pruebas incluidas.
- [ ] HU56/HU57: emitir `CONTRACT_SYNC` a Console (`node harness/contract-sync.mjs publish`) al aprobar el contrato y otro al terminar la implementación, para que Console reemplace el `PendingContractError` de `enable` y `DELETE` por el adapter live. Nunca se edita el repositorio de Console.
