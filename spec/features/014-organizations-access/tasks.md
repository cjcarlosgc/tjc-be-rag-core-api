# 014 — Tareas

Todas sin iniciar. Requiere aprobación humana del work item (`AWAITING_APPROVAL`); `DEC-ORG-001` y `DEC-ORG-002` están aprobados y no bloquean. Contrato consolidado en SYSTEM-2.4 / INTEROP-2.4.

**Regla de CONTRACT_SYNC (única):** un sync de definición al aprobar el contrato (lo emite el leader) y un sync de implementación por cada bundle desplegable: bundle A = corte 1 + 4a; bundle B = cortes 2 + 3 (con 4b) + 5. Nunca uno por corte; nunca se edita el repositorio de Console.

**Orden de publicación:** la Console solo GitHub primero; bundle A después (espera a esa publicación porque `GITHUB_IDENTITY_REQUIRED` aplica a toda la sesión; la App no debe ser instalada por terceros, idealmente privada, hasta desplegarlo); bundle B (2, 3, 5) siempre junto, y solo tras su sync de implementación la Console envía `workspaceId` a `POST /projects` y `GET /projects` y consume `workspace`/`role`.

## Corte 1 — HU62 + identidad (bundle A)

- [x] Migración `user_github_identities` (sin backfill) y repositorio. Evidencia: `app/prisma/migrations/20260921120000_user_github_identities/migration.sql` (generada con `prisma migrate diff`, **escrita y NO aplicada a Supabase**), `UserGithubIdentitiesRepository`.
- [x] `SupabaseIdentityPort` + adapter (Admin API por `sub`, `identities[].id` del provider `github`) + fake de tests. Evidencia: `app/src/common/auth/supabase-admin-identity.adapter.ts` y `app/test/support/fake-supabase-identity.port.ts`.
- [x] Resolver y persistir `githubUserId` tras validar el JWT (HTTP y handshake WebSocket por la misma vía); nunca leer `user_metadata`. Evidencia: `GithubIdentityService`, `SessionAuthService` (guard HTTP/WS y `io.use()` del `RealtimeGateway` con `err.data`).
- [x] `AUTH_BYPASS` de desarrollo con identidad GitHub sintética configurada (`AUTH_BYPASS_GITHUB_USER_ID`), sin llamar a Supabase y rechazado en producción (validación de entorno y resolvedor).
- [x] `401 GITHUB_IDENTITY_REQUIRED` y `503 IDENTITY_UNAVAILABLE` con el envelope estándar (HTTP y rechazo de handshake WebSocket).
- [x] Pruebas (identidad presente/ausente, metadata manipulada, Admin API caída con y sin vínculo, bypass). Evidencia: specs de `common/auth`, `realtime.gateway.spec.ts`, `env.validation.spec.ts` y `test/github-identity.e2e-spec.ts`.
- [x] Actualizar `012-web-authentication` (tareas de calidad: login solo GitHub; correo deshabilitado tras la Console; manual linking deshabilitado). Las tareas de despliegue quedaron registradas en `012-web-authentication/tasks.md` sin marcar.

## Corte 4a — HU64, la corrección de seguridad (bundle A; justo tras el corte 1)

- [x] `GithubAccessPort.getRepositoryOwner`/`getRepositoryPermission` (resolviendo el login por `GET /user/{id}`) con fake y adapter HTTP; resultado `UNVERIFIABLE` distinto de `null`. Evidencia: `app/src/github-app/github-access.port.ts` (resultado `OK | NOT_FOUND | NOT_INSTALLED | UNVERIFIABLE`; los métodos reciben `{ installationId, repositoryName }` porque las lecturas de GitHub se direccionan por `owner/repo` con el installation token), `github-access-http.adapter.ts` y `app/test/support/fake-github-access.port.ts`.
- [x] `POST .../integrations/github` con el orden de INTEROP §6.8: sin permiso -> `404 GITHUB_REPOSITORY_NOT_FOUND` antes de `REPOSITORY_OUTSIDE_WORKSPACE`; `400 REPOSITORY_OUTSIDE_WORKSPACE` (personal: propietario = creador); `403 REPOSITORY_PERMISSION_INSUFFICIENT`; `409 REPOSITORY_ALREADY_BOUND` solo después; `503` ante permiso no verificable.
- [x] `verify-app-access` y `branches` con permiso mínimo `maintain`/`write`/`admin` (`403`; sin visibilidad `NOT_AUTHORIZED` y `404`; no verificable `503`).
- [x] `POST .../enable` sobre un binding `REVOKED` valida propietario (personal) y `repositoryId` como `POST` binding (`404 GITHUB_REPOSITORY_NOT_FOUND`, `400 REPOSITORY_OUTSIDE_WORKSPACE`; sigue `REVOKED`); la rama de organización va en el corte 3.
- [x] Orden en `branches` (App no instalada `403` antes de los `404`/`403` de permiso) y en `verify-app-access` (`NOT_AUTHORIZED`) con la App no instalada.
- [x] `workspaceId` en `GET /integrations/github/repositories` solo con el workspace personal; el de organización responde `404 WORKSPACE_NOT_FOUND` hasta el corte 3.
- [x] Pruebas del orden de validación y de no sondeo de repositorios ajenos; regresión de `013`. Evidencia: specs de `repository-bindings/` y `github-app/`, `test/repository-access.e2e-spec.ts` (HTTP: orden de `POST` binding, `enable` sobre `REVOKED`, `verify-app-access`, `branches`, discovery).

## Corte 2 — HU63 + HU58 (bundle B; paso de integración)

- [x] Migración `projects.githubOrgId`/`githubOrgLogin` con check de coherencia e índice. Evidencia: `app/prisma/migrations/20260921140000_project_workspace_columns/migration.sql` (**escrita y NO aplicada a Supabase**; validada en un PostgreSQL local descartable: personal y organización aceptan, una sola columna se rechaza) y `app/src/prisma/project-workspace-migration.spec.ts`.
- [x] `GithubAccessPort` del lado de organización (instalaciones, membresía, owners) con fake y adapter HTTP. Evidencia: `listOrganizationInstallations`, `getOrganizationMembership` y `listOrganizationOwners` en `app/src/github-app/github-access.port.ts` y `github-access-http.adapter.ts` (con `OrganizationRef { installationId, organizationLogin }`, misma convención `OK | NOT_FOUND | NOT_INSTALLED | UNVERIFIABLE`) y en `app/test/support/fake-github-access.port.ts`.
- [x] `GET /workspaces` con degradación ante caída de GitHub a solo el personal (el respaldo de organizaciones con acceso registrado llega con el corte 3). Evidencia: `app/src/workspaces/` (`WorkspacesService`, `WorkspacesController`), `workspaces.service.spec.ts` y `test/workspaces.e2e-spec.ts`.
- [x] `POST /projects` y `GET /projects` con `workspaceId` solo personal (omitido o el propio id); el discovery no cambia en este corte; un `workspaceId` de organización responde `404 WORKSPACE_NOT_FOUND` (`assertPersonalWorkspace`); `DELETE` como creador (comportamiento de HU56 sin cambios, cubierto por sus pruebas existentes). Evidencia: `app/src/projects/`, `app/src/workspaces/personal-workspace.util.ts`, `test/projects.e2e-spec.ts`.
- [x] `PATCH /projects/{projectId}` (renombrar; solo Admin) **diferido en el corte 2 e implementado en el corte 3 (etapa 2a)**; pruebas: nombre válido, vacío, campo extra, no visible y `403` a Maintainer/Reader. Evidencia: `ProjectsController.update`/`ProjectsService.update`, `projects.service.spec.ts` y `test/org-access.e2e-spec.ts`; la matriz de la etapa 2b lo cubre en `test/access-matrix.e2e-spec.ts`.
- [x] `ProjectResponse` con `workspace` y `role` (interino: `ADMIN`, predicado del creador). Evidencia: `app/src/projects/projects.service.ts` (`toResponse`) y `test/projects.e2e-spec.ts`.
- [x] Pruebas del corte y de contrato. Evidencia: `workspaces.service.spec.ts`, `github-access-http.adapter.spec.ts`, `projects.service.spec.ts`, `projects.controller.spec.ts`, `personal-workspace.util.spec.ts`, `concurrency.util.spec.ts`, `test/workspaces.e2e-spec.ts` y `test/projects.e2e-spec.ts` (sin `PATCH`, diferido).

## Corte 3 — HU59 + HU60 + HU64 parte 4b (bundle B)

- [x] Migración `project_access` + enum `ProjectRole` (solo Projects de organización), con inserción del registro `ADMIN` del creador en la transacción de `POST /projects` en una organización; un Project personal no crea registro. Evidencia: `app/prisma/migrations/20260921150000_project_access/migration.sql` (**escrita y NO aplicada a Supabase**; con RLS y REVOKE condicional; validada en un PostgreSQL 14 local descartable: RLS activo, PK `(projectId, userId)` rechaza duplicados, FK con cascada), `ProjectsRepository.createInOrganization` (Project + registro `ADMIN` en la misma transacción), `rls-guard.spec.ts` y `projects.repository.spec.ts`.
- [x] `accessibleProject(userId, minRole)` (rama personal por `ownerUserId`; rama de organización por registro suficiente Y un binding que EXISTA y no esté `REVOKED`, o rol `ADMIN`, evaluada en cada petición; corregido en la etapa 2b: un Project sin repositorio lo ve solo un Admin) con prueba de que un registro Maintainer/Reader no da acceso con el binding `REVOKED` ni sin binding, y `rolesAtLeast`; sustituir todos los usos de `ownedProject` (projects, versions, targets, runs, functional knowledge, questions, publications, bindings, experiments). Evidencia: `app/src/common/persistence/accessible-project.filter.ts` (sustituye a `ownedProject`, eliminado), `accessible-project.filter.spec.ts` (el predicado evaluado sobre datos: REVOKED en petición, jerarquía, personal ajeno, borrado) y `accessible-project-scope.spec.ts` (todos los repositorios de projects, versions, targets, runs, Functional Knowledge, preguntas, publicaciones, bindings y experimentos usan el predicado).
- [x] `ProjectAccessService` (alta al entrar sin carreras: UN advisory lock transaccional por `(projectId, userId)`, el mismo que toman reverificaciones y revocaciones, acotado por el presupuesto de verificaciones; `404`/`403`/`503`) con tope de concurrencia y presupuesto por petición, sin memoizar denegaciones. Evidencia: `app/src/project-access/` (`ProjectAccessService`, `ProjectAccessRepository.withAccessLock`, `OrganizationAccessResolver` con el login vigente de la instalación) y `project-access.service.spec.ts` (incluida la prueba de carrera alta vs revocación con el lock por `(projectId, userId)`, presupuesto y concurrencia). El alta por deep link a un recurso descendiente (`requireForResource`) llegó con la etapa 2b (ver abajo).
- [x] Guard default-deny y decoradores (`@RequireProjectRole`, `@NoProjectRole`) aplicados a cada ruta según la matriz de INTEROP §6.13; prueba que enumera el router y falla ante rutas sin declaración. Evidencia (etapa 2b): `app/src/project-access/access-policy.ts` (decoradores y `ProjectTargets`), `project-role.guard.ts` (`ProjectRoleGuard`, APP_GUARD; deniega una ruta sin declaración con `500 INTERNAL_ERROR` y falla cerrado con `401` sin identidad), `route-access-audit.ts` (`listRouteAccess` y `RouteAccessAuditor`, que impide ARRANCAR con una ruta sin declaración), los controladores con su rol por ruta, `project-role.guard.spec.ts`, `default-deny.spec.ts` (ruta sin declaración denegada y arranque abortado) y `test/access-matrix.e2e-spec.ts` (enumera todas las rutas HTTP y el handshake y los mensajes WebSocket del router real y falla ante cualquiera sin declaración; compara el rol implementado con la matriz de INTEROP §6.13 ruta por ruta, con la fila de cada rol del contrato leída del propio `interoperability-contract.md`).
- [x] Alta por deep link a recursos descendientes (Run, versión, pregunta, publicación, experimento, target): `ProjectAccessService.requireForResource` resuelve el `projectId` del recurso (`ProjectAccessRepository.findProjectIdOf`, sin filtro de visibilidad y devolviendo solo el id) y aplica `require`, con el mismo predicado y el `404` PROPIO del recurso (idéntico al de un id inexistente; nunca `PROJECT_NOT_FOUND` en una ruta de recurso descendiente). Evidencia: `project-access.service.spec.ts` (los seis tipos de recurso, `403` con `details`, `404` sin fuga, Project personal ajeno sin llamar a GitHub, Project borrado, `503`) y `test/access-matrix.e2e-spec.ts` (entrada por Run, versión, publicación, experimento y preguntas sin abrir antes el Project; colaborador externo `404` sin registro; GitHub caído `503`).
- [x] Derivación de rol: owner de organización; membresía activa SIEMPRE (colaborador externo con `write` denegado, repos privados, internal y públicos) y permiso de repositorio para Maintainer/Reader; visibilidad de Project sin repositorio y con binding `REVOKED` solo a Admin. Evidencia: `project-access.service.spec.ts` y `test/org-access.e2e-spec.ts` (owner = ADMIN; membresía activa siempre; colaborador externo con `write` denegado; `maintain`/`write`/`admin` = MAINTAINER, `triage`/`read` = READER; sin repositorio o REVOKED solo Admin).
- [x] 4b: creación de Projects en organización (`WORKSPACE_ADMIN_REQUIRED`), `workspaceId` de organización en `POST /projects`, `GET /projects` y discovery; rama de organización de `REPOSITORY_OUTSIDE_WORKSPACE`; rol Maintainer en binding/`enable`/pausa (reactivar `REVOKED` lo hace un Admin). Sin ninguna tarea duplicada con el 4a. Evidencia: `ProjectsService.create` (`WORKSPACE_ADMIN_REQUIRED`, `WORKSPACE_NOT_FOUND`, `503`), `GET /projects?workspaceId`, discovery con organización (`organizationOwnerId`), rama de organización de `REPOSITORY_OUTSIDE_WORKSPACE`, `PATCH`/`DELETE` solo Admin y Maintainer en binding/pausa/`enable` (specs de `projects/`, `repository-bindings/` y `test/org-access.e2e-spec.ts`).
- [x] `GET /workspaces`: respaldo con las organizaciones de Projects con acceso registrado ante una caída de GitHub; una organización con la App desinstalada (`NOT_INSTALLED`) no se ofrece y sus Projects responden `404`, no `503`. Evidencia: `WorkspacesService` (respaldo con organizaciones de acceso registrado; `NOT_INSTALLED` no se ofrece), `workspaces.service.spec.ts` y `test/org-access.e2e-spec.ts`.
- [x] `POST .../enable` sobre `REVOKED`, rama de organización (propietario = organización del Project y `repositoryId`), reactivado por un Admin. Evidencia: `RepositoryBindingsService.enable` (propietario esperado = `githubOrgId` del Project), `repository-bindings.service.spec.ts` y `test/org-access.e2e-spec.ts`.
- [x] Listados: `GET /projects`, `GET /analysis-runs` y `GET /action-required` (personales del usuario más organización con registro); `GET /action-required?projectId` de un Project no visible responde `404 PROJECT_NOT_FOUND`. Evidencia: `GET /projects` (alta de candidatos con presupuesto), `GET /analysis-runs` (ruta nueva de HU55) y `GET /action-required` (`projectId` no visible -> `404`); `analysis-runs.service.spec.ts`, `functional-knowledge.service.spec.ts` y `test/org-access.e2e-spec.ts`.
- [x] `RealtimeGateway`: mapa socket -> Project, rol Reader al suscribirse, `SubscribeAck` (rechazo reintentable con `GITHUB_VERIFICATION_UNAVAILABLE`), expulsión por pérdida de acceso, borrado lógico y binding `REVOKED` para no Admin. Evidencia (etapa 2b): `app/src/realtime/realtime.gateway.ts` (`subscribe:project-version` con `ProjectAccessService.requireForResource(..., 'READER')` y los tres resultados exactos del `SubscribeAck`; `handleDisconnect`; handshake sin cambios declarado con `@WsHandshakeAccess`), `project-subscriptions.service.ts` (`ProjectSubscriptionsService`: mapa socket -> Project; `revalidateProject(projectId)` expulsa con el predicado `accessibleProject` a quien ya no ve el Project por registro borrado, borrado lógico o binding `REVOKED` para no Admin; `evictUser`; invocable por el corte 5 y ya conectado a `DELETE /projects/{id}`), `realtime.gateway.spec.ts`, `project-subscriptions.service.spec.ts` y `test/access-matrix.e2e-spec.ts` (Reader/Maintainer/Admin suscritos, no miembro y externo `{false,null,false}`, GitHub caído `{false,GITHUB_VERIFICATION_UNAVAILABLE,true}`, expulsión por los tres motivos).
- [x] Matriz e2e ruta x rol, pruebas de caída de GitHub y **prueba de carrera alta vs revocación**. Evidencia: `test/access-matrix.e2e-spec.ts` (22 rutas con recurso x Admin/Maintainer/Reader/no miembro/colaborador externo: `404` propio del recurso, `403 PROJECT_ROLE_INSUFFICIENT` con `details` o permitido; el mismo `404` que un id inexistente; los tres listados; Project personal en cada ruta; un owner que no es colaborador explícito entra como Admin y configura un binding; Maintainer sin crear/renombrar/eliminar), `test/org-access.e2e-spec.ts` (caída de GitHub y App desinstalada) y la carrera alta vs revocación en `project-access.service.spec.ts` (advisory lock por `(projectId, userId)`) y a nivel HTTP por deep link en la matriz e2e.

## Corte 5 — HU61 (bundle B)

5a (implementado sobre el corte 3 ya fusionado: incluye el borrado de registros Maintainer/Reader al pasar un binding a `REVOKED`; migración escrita y NO aplicada a Supabase):
- [x] `repository.renamed`/`transferred`/`deleted` (y `privatized`, reconocido sin efecto hasta el 5b) sobre el binding de CUALQUIER Project, personales incluidos, y ajustes de `installation`/`installation_repositories` sobre bindings (`suspend` no borra; `deleted`/`removed` -> `REVOKED` y borrado de registros Maintainer/Reader con la expulsión de sockets); se procesan aunque el binding no esté `ENABLED`; `member`, `membership`, `organization` y `team` siguen respondiendo `202` e ignorándose. Evidencia: `app/src/github-webhooks/repository-events.service.ts` y `github-webhooks.service.ts`, `app/src/access-sync/binding-lifecycle.service.ts` (`revokeBinding`: estado `REVOKED` ANTES de borrar, `ProjectAccessService.revoke` por usuario con su advisory lock, `ProjectSubscriptionsService.revalidateProject`), `repository-events.service.spec.ts`, `github-webhooks.service.spec.ts`, `binding-lifecycle.service.spec.ts` y `test/access-webhooks.e2e-spec.ts` (por evento y `action`, duplicados, fuera de orden, firma alterada, binding `DISABLED`, Project personal).
- [x] Infraestructura de jobs `ACCESS_RECONCILIATION`/`ACCESS_REVERIFY`: migración de `jobs.dedupeKey` y del índice único parcial solo sobre `PENDING`, reclamo (`claimNext`, una sola sentencia) que no toma un `PENDING` mientras exista un `RUNNING` no obsoleto con la misma `dedupeKey`, reclamo de locks obsoletos (`JOBS_STALE_LOCK_MS`, solo jobs con `dedupeKey`), nota N1 en `fail`/`reschedule`, `enqueueDeduped` con siembra (`skipIfRunning`) y `RescheduleJobError`/`accessBackoffMs` (backoff creciente acotado a una hora, sin consumir `maxAttempts`; lo usa `ACCESS_REVERIFY` en el 5b). Evidencia: `app/prisma/migrations/20260921160000_jobs_dedupe_key/migration.sql` (**escrita y NO aplicada a Supabase**; validada en un PostgreSQL 14 local descartable), `app/src/jobs/jobs.repository.ts`, `jobs.service.ts`, `reschedule-job.error.ts`, `jobs.repository.pg.spec.ts` (SQL real contra un PostgreSQL local con `JOBS_TEST_DATABASE_URL`; se omite sin ella), `jobs.service.spec.ts`, `access-jobs.queue.spec.ts` (evento durante un `ACCESS_REVERIFY` `RUNNING`, lock obsoleto, N1, backoff) y `jobs-dedupe-migration.spec.ts`.
- [x] La reconciliación (c): cadena autoencolada AL INICIO (now + 1 h, sobrevive a un fallo), siembra al arrancar solo sin `PENDING` ni `RUNNING` no obsoleto, presupuesto de verificaciones y concurrencia por ejecución con cursor a la siguiente ocurrencia, y revalidación de propietario y nombre por id de todo Project vivo con binding (personales incluidos): transferido/eliminado/App sin acceso -> `REVOKED` y borrado de Maintainer/Reader con expulsión de sockets; renombrado -> `repositoryName`; no verificable -> conserva y reintenta (nunca revoca por un error de red); termina las revocaciones interrumpidas. Evidencia: `app/src/access-sync/access-reconciliation.job-handler.ts` (+ `.spec.ts`), `GithubAccessPort.getRepositoryById` (fake y adapter HTTP con `github-access-http.adapter.spec.ts`), `access-revocation-race.spec.ts` (carrera alta vs evento de revocación en su lado de eventos: el alta confirma el binding con `FOR SHARE` antes del upsert) y `test/access-webhooks.e2e-spec.ts`.

5b (depende del 3 y del 5a):
- [x] Enrutar `member`, `membership`, `organization`, `team` y `repository.privatized` en el ingress (firma HMAC, `202` y shape sin cambios, sin `WebhookDelivery`); encolar `ACCESS_REVERIFY` con el alcance del payload (el payload solo selecciona, el rol sale de una verificación viva bajo el advisory lock). Evidencia: `app/src/github-webhooks/access-events.service.ts` (`member` -> `USER_REPOSITORY`; `membership` -> `USER_ORGANIZATION_REPOSITORIES`; `organization.member_removed` -> `USER_ORGANIZATION`; `team` -> `REPOSITORY` con `repository.id` o `ORGANIZATION_REPOSITORIES` sin él; `member_added`/`member_invited`/`created` y los demás `action` se ignoran; un evento sin ids o sin Project de organización vivo no encola), `repository-events.service.ts` (`privatized` -> `REPOSITORY`), `github-webhooks.service.ts`, `app/src/access-sync/access-reverify.scope.ts` (alcances y `dedupeKey`), `access-reverify.service.ts` (`enqueue` con `enqueueDeduped` y adelanto de un `PENDING` en backoff con `JobsRepository.expedite`, SQL validado en PostgreSQL local), `access-reverify.job-handler.ts` (job `access-reverify`: borra lo perdido, actualiza el rol, conserva lo no verificable con `RescheduleJobError` + `accessBackoffMs`, alcance completo, sin consumir `maxAttempts`), `ProjectAccessService.reverify` (mismas reglas que el alta, mismo lock, `FOR SHARE` del binding) y `revalidateProject` tras cambiar registros; specs `access-events.service.spec.ts`, `access-reverify.job-handler.spec.ts`, `repository-events.service.spec.ts`, `github-webhooks.service.spec.ts` y `test/access-webhooks.e2e-spec.ts`.
- [x] Efectos de organización con `projects.githubOrgId/githubOrgLogin`: `organization.renamed` (actualiza el login guardado) y `organization.deleted` (Projects ocultos y conservados). Evidencia: `app/src/access-sync/organization-lifecycle.service.ts` (`rename`, `hide`), `ProjectAccessRepository.updateOrganizationLogin`/`findLiveProjectIdsOfOrganization`, `organization-lifecycle.service.spec.ts`, `access-events.service.spec.ts` y e2e.
- [x] Ciclo de vida de la organización (desaparece, App desinstalada o sin owners: binding `REVOKED` y borrado de TODOS los registros, Admin incluido, sin borrar Projects ni evidencia; `installation.deleted` de una organización, con `account.type`) y reaparición con binding `REVOKED` reactivado por un Admin. Evidencia: `BindingLifecycleService.hideProject`, `OrganizationLifecycleService.hide`, `GithubWebhooksService` (`installation.deleted` con `account.type === 'Organization'`), pruebas de reaparición (el Admin recupera el Project al entrar, Maintainer/Reader solo tras `enable`) en `organization-lifecycle.service.spec.ts`, `access-reconciliation.job-handler.spec.ts` y `test/access-webhooks.e2e-spec.ts`.
- [x] Reconciliación (a) y (b) de registros de acceso: mismas reglas que el alta, borra lo confirmado perdido y conserva lo no verificable. Evidencia: `app/src/access-sync/access-reconciliation.job-handler.ts` (`reconcileOrganizations` (a): resoluble, App instalada, con owner, corrige el login vigente; `reconcileRecords` (b): un Project completo por vez con `ACCESS_RECONCILIATION_BUDGET` y cursor `afterProjectId`; una lectura de la lista de instalaciones por ejecución; una parte que falla no impide las demás; orden (a), (c), (b)), `OrganizationLifecycleService.reconcile`, `access-reconciliation.job-handler.spec.ts` y `test/access-webhooks.e2e-spec.ts` (rol cambiado sin evento, App desinstalada sin evento, GitHub caído).
- [x] Pruebas por evento y `action`, duplicados/fuera de orden, firma alterada, jobs (dedupe solo `PENDING`, lock obsoleto, siembra, cadena que sobrevive a un fallo, evento durante un `ACCESS_REVERIFY` `RUNNING`), reconciliación, caída de GitHub y **carrera alta vs evento de revocación**. Evidencia: `access-events.service.spec.ts` (cada evento y `action`, dedupe, evento durante un `RUNNING`, adelanto del backoff), `access-reverify.job-handler.spec.ts` (alcances, rol vivo, backoff creciente acotado a una hora sin consumir intentos, cadena, evento durante `RUNNING` y la carrera: una reverificación de un usuario corre DESPUÉS de un alta en vuelo y borra su registro obsoleto; verificado que la prueba falla si la reverificación solo mirase registros existentes), `access-jobs.queue.spec.ts`/`access-reconciliation.job-handler.spec.ts` (siembra, lock obsoleto, cadena), `jobs.repository.pg.spec.ts` (`expedite`) y `test/access-webhooks.e2e-spec.ts` (ingress real con firma, `202`, cola en memoria y efecto observado por HTTP).

- [x] Cierre del ciclo de corrección 1 del bundle B (hallazgos de la revisión de jobs/webhooks; los revisores de modelo de acceso y contrato aprobaron). Evidencia: (1) una lista de owners VACÍA con `200` es `UNVERIFIABLE` (`github-access-http.adapter.ts`, `OrganizationLifecycleService.reconcile`; solo se oculta con `NOT_FOUND`/`NOT_INSTALLED`, `organization.deleted` o `installation.deleted`); (2) la cola usa `(now() AT TIME ZONE 'utc')` en `insertDeduped`/`claimNext`/`releaseStale`/`expedite` y `jobs.repository.pg.spec.ts` corre con la sesión en `UTC` y en `America/Bogota` (con `now()` fallan 7 casos en Bogota); (3) `installation.deleted` e `installation_repositories.removed` aíslan el fallo por binding, ejecutan `hide()` al final, registran los parciales y responden `202`; (4) `ACCESS_REVERIFY` reprograma sin consumir intentos solo por `unverifiable` y un `failed` sigue el camino `fail` hasta `FAILED`; (5) `getAllPages` devuelve `UNVERIFIABLE` al alcanzar el tope; (6) `CreateProjectDto` recorta el nombre (`create-project.dto.spec.ts`, e2e); (7) coherencia `role`/`row` del fixture de la matriz (`access-matrix.e2e-spec.ts`); (8) `subscribe:project-version` registra antes de unir y revalida después (`ProjectSubscriptionsService.revalidateSocket`, `realtime.gateway.spec.ts`); (9) límites conocidos en `plan.md`.

Cortes 3 (etapas 2a y 2b) y 5 (5a y 5b): completos; el bundle B (cortes 2, 3 y 5) está implementado. Queda por aplicar las migraciones (escritas y NO aplicadas a Supabase), la revisión independiente y el `contract-reviewer`, el `CONTRACT_SYNC` de implementación y la validación contra una organización real (sección "Cierre").

## Cierre

- [ ] Lint, test y build; e2e de la matriz; revisión independiente y `contract-reviewer`.
- [ ] `CONTRACT_SYNC` a Console: el de definición al aprobar el contrato (leader) y uno de implementación por bundle (A y B), con las rutas, DTOs, errores, orden de despliegue y `breaking: false` cualificado.
- [ ] Registrar la evidencia de las precondiciones de despliegue (Console solo GitHub publicada antes del bundle A, bundle A antes de que terceros instalen la App (hasta entonces no instalada por terceros, idealmente privada), App pública con `Members: read` y eventos, validación contra una organización real incluido un colaborador externo, manual linking deshabilitado, correo deshabilitado tras la Console solo GitHub) como criterio de aceptación, sin bloquear el merge.

## Notas de implementación de la revisión contractual (2026-09-21, no requieren nuevo ciclo)

- Jobs (corte 5a): al reprogramar un `ACCESS_REVERIFY` cuyo `dedupeKey` ya tiene otro `PENDING` (por backoff de "no verificable" o por reclamo de un lock obsoleto), completar o descartar el job actual en lugar de devolverlo a `PENDING`, para no chocar con el índice único parcial.
- Reclamo de jobs: `claimNext` debe excluir un `PENDING` cuyo `dedupeKey` coincida con un `RUNNING` no obsoleto (una sola sentencia; los tipos sin `dedupeKey` no cambian). El reclamo de `RUNNING` obsoletos es infraestructura nueva del corte 5a.
- `repository.transferred` y la reconciliación (c) para Projects de organización comparan con `projects.githubOrgId` (columna del corte 2): esa rama se implementa en el corte 5b. Hasta el corte 3 solo existen Projects personales.
- WebSocket (corte 1): añadir la tarea de rechazo en el handshake con `io.use()` y `err.data` (`GITHUB_IDENTITY_REQUIRED`, `IDENTITY_UNAVAILABLE`, `INVALID_ACCESS_TOKEN`). Hoy la autenticación es un guard por mensaje. Un rechazo de middleware desactiva la reconexión automática del cliente, así que `retryable: true` exige `connect()` manual; la Console actual no usa `auth` y cae al polling.

