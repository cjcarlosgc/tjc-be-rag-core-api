# Progreso actual

Sprint 1 completo (HU01-HU07) en `app/`: gestión de proyectos, indexación asíncrona completa (ZIP → snapshot en Object Storage → extracción segura → parsing ts-morph → chunking → embeddings OpenAI → pgvector) e inventario de pruebas (targets CLASS/METHOD/FUNCTION, detección de framework, resolución heurística de cobertura existente). Cola de jobs DB-backed. Lint/test/build en verde. Evidencia en `harness/reports/`.

Object Storage migrado de MinIO/S3-compatible a **Supabase Storage** (`@supabase/supabase-js`) detrás de la abstracción `ObjectStorageService`, con bucket-check perezoso (no bloquea el arranque de la app sin credenciales reales). Migración de código completa y verificada (unit + e2e con fake en memoria); ver `harness/reports/object-storage-supabase-migration.md`. `docker-compose.yml` ya no incluye MinIO.

Decisiones PENDING resueltas en el camino: package manager (pnpm), mecanismo durable de jobs (cola DB-backed en PostgreSQL), proveedor de Object Storage (Supabase Storage, sustituyendo la elección inicial de MinIO).

Cambio de contrato SDD 1.0 → 1.1: `TestTarget.targetType` ahora es `CLASS|METHOD|FUNCTION`; el modo de generación puntual de `005-test-generation` se renombra de `METHOD` a `TARGET`. SDD 1.1 → 1.2: proveedor de Object Storage a Supabase Storage. SDD 1.2 → 1.3: TypeScript-only explícito, baseline sustituido por agente generalista, validación empresarial y puertas de decisión para embeddings, chunking, mutation score y test-aware. Ver `CHANGELOG.md`.

Fix aplicado: si falla la subida del snapshot, `setSnapshot` o el encolado del job, la `ProjectVersion` se marca `FAILED` en vez de quedar huérfana en `PENDING` (evita bloqueo permanente por 409 en reintentos). Verificado con pruebas unitarias y manualmente contra Supabase inalcanzable; detalle en `harness/reports/object-storage-supabase-migration.md`.

Auditoría de completitud actualizada: se documentó el comportamiento real de servicios/chunking de Sprint 1, pero la estrategia definitiva para retrieval continúa en `DEC-CHUNK-001`. Los transversales de providers y métricas ya declaran sus reglas y puertas principales; otros contratos operativos se refinan cuando el work item los necesite. Ver `harness/reports/sdd-1.3-decision-consolidation.md`.

En el corte SDD 1.4 se incorporó `SYSTEM-1.0`, se corrigieron HU06/HU08 para incluir funciones y `DEC-INT-001` registró entonces el contrato HTTP Core↔Sandbox como pendiente.

SDD 1.5 / SYSTEM-1.1 incorpora `INTEROP-1.0`, contrato universal con rutas, DTOs, errores, asincronía, artefactos y ejecución Sandbox. `DEC-INT-001` queda aprobado; no se cierran las puertas de retrieval, agente generalista, mutation testing, package managers o validación empresarial.

SDD 1.6 queda homologada como línea base conjunta de los tres repositorios y formaliza la entrega Git común: cada commit es un cambio coherente con `Refs: HU...`; cada work item conserva su revisión y, antes del push de cierre de sprint, el reviewer debe aprobar y documentar el rango acumulado exacto que se publicará. `SYSTEM-*` e `INTEROP-*` conservan versionado propio. Commit y push continúan requiriendo solicitud humana explícita.

SDD 1.7 / SYSTEM-1.2 aprueba como entorno temporal de desarrollo y prevalidación la MacBook encendida con Docker Desktop y su VM Linux. El destino previsto continúa siendo una VM Linux remota, pero `DEC-INF-001` mantiene `PENDING` la selección del proveedor, priorizando opciones gratuitas sin asumir que cumplen capacidad, disponibilidad o seguridad. INTEROP-1.0 no cambia.

SDD 1.8 / SYSTEM-1.3 / INTEROP-1.1 consolida la frontera de datos: solo RAG Core usa Supabase Storage/PostgreSQL/pgvector, genera URLs firmadas temporales para que Sandbox descargue inputs y persiste sus resultados. Sandbox y Frontend no reciben credenciales Supabase/DB ni incorporan el SDK por esta infraestructura. Se fija el proyecto Supabase, el bucket privado `repository-zips`, las variables canónicas, cargas sin sobrescritura y migraciones Prisma como mecanismo de esquema; no se ejecutaron migraciones remotas.

SDD 1.9 resuelve `DEC-EMB-001`, `DEC-CHUNK-001` y `DEC-EXP-002` (las tres `APROBADO`): modelo de embeddings definitivo (`text-embedding-3-small`, 1536), diseño de chunking jerárquico + oversized structured chunks para `002-project-version-indexing`/`004-rag-retrieval-context`, y contrato operativo del `GENERALIST_AGENT` para `008-experimental-comparison`/HU19. Ninguna resolución tocó código en `app/`. Se aclara además `DEC-INF-001` (sigue `PENDING`): Docker Desktop local confirmado suficiente para Sprint 2-4, proveedor remoto se revisita después de Sprint 4.

Siguiente paso sugerido: implementar en orden (a) el rework de chunking en `002-project-version-indexing` (jerárquico + oversized + tokenCount real) sobre el código ya-DONE de Sprint 1, (b) `004-rag-retrieval-context` (retrieval/ContextBuilder) y (c) `008-experimental-comparison`/HU19 (GENERALIST_AGENT), en ese orden, ya que 004 depende de 002 y 008 depende de 004. `DEC-RAG-001` y `DEC-MET-001` siguen como análisis deseados inmediatamente después del núcleo de Sprint 2 y no lo bloquean. La integración Sandbox ya dispone de contrato; la validación empresarial continúa sujeta a `DEC-VAL-001`.

(a) y (b) ya están implementados y verificados en `app/`: chunking jerárquico + oversized structured chunks + `importsUsed` + `tokenCount` real (`gpt-tokenizer`), migración `20260906053903_chunk_hierarchical_design` aplicada contra la Supabase real, y `src/retrieval/` (`RetrievalService`/`ContextBuilder`). `DATABASE_URL` requiere `uselibpqcompat=true` junto a `sslmode=require` para que `@prisma/adapter-pg` valide correctamente el certificado de Supabase (fix aplicado en `.env`/`.env.example`; sin este parámetro el driver trata `require` como `verify-full` y falla con "self-signed certificate in certificate chain"). Evidencia en `harness/reports/004-rag-retrieval-context.md`.

`005-test-generation` + `006-validation-orchestration` + `007-artifacts` (HU08-HU18) también están implementados: `POST /test-runs` → `GapAnalyzer` resuelve targets → retrieval/context (004) → `LLMProvider` genera → `TestFileMergeService` aplica CREATE/MERGE con ts-morph → `SandboxExecutionService` valida (contrato `INTEROP-1.1` completo: URLs firmadas efímeras, idempotencia, correlación, polling) → `ArtifactService` persiste/entrega (diff, download individual/ZIP). Nuevas tablas `test_generation_runs`/`target_run_results`/`artifacts` (migraciones `20260906060628_generation_pipeline`, `20260906062305_target_run_result_test_file_path`, aplicadas contra Supabase). Lint/test (125/125)/test:e2e (9/9)/build en verde; evidencia y limitaciones documentadas en `harness/reports/005-006-007-generation-pipeline.md` — en particular: **no hay verificación contra un Sandbox real** (otro repositorio, ausente en este workspace; el cliente HTTP está probado con fakes/mocks pero nunca contra el servicio real), `evidenceIds` siempre `[]` (evidencia stdout/stderr no persistida) y la validación batch final del run no se implementó (solo validación por target individual). Fix de infraestructura no relacionado encontrado en el camino: `vitest.config.e2e.ts` necesitaba `fileParallelism: false` porque los e2e comparten la tabla `jobs` real de Postgres entre archivos.

`008-experimental-comparison`/HU19 también está implementado, cerrando Sprint 2 completo (HU08-HU19) en código: `POST /experiments` ejecuta `ExperimentJobHandler` (2 estrategias × 3 repeticiones = 6, workspace fresco por repetición). Brazo RAG reutiliza el pipeline de `005` (`RetrievalService`/`ContextBuilder`/`PromptBuilder`/`LLMProvider`). Brazo `GENERALIST_AGENT`: `WorkspaceAgentTools` (4 herramientas read-only: listar/leer/buscar texto/inspeccionar símbolo, excluyendo los tests existentes del target) + `GeneralistAgentService` (loop de tool-calling real de OpenAI, trayectoria completa persistida). Ambos comparten timeout de generación (`GENERATION_TIMEOUT_MS`) y validación en el Sandbox (`mapSandboxResult`, extraído a `src/sandbox/` para reuso entre 005 y 008). Nuevas tablas `experiment_runs`/`experiment_repetitions` (migración `20260906140641_experiment_comparison`, aplicada contra Supabase). Lint/test (161/161)/test:e2e (11/11)/build en verde; evidencia y limitaciones en `harness/reports/008-experimental-comparison.md` — misma limitación transversal: sin Sandbox ni OpenAI reales disponibles en este workspace.

Sprint 2 (HU08-HU19) completo en código. Pendiente transversal a `004`-`008`: verificación end-to-end contra el Sandbox real (otro repositorio) y contra la API real de OpenAI.

Al cerrar Sprint 2 se revisaron pendientes reales de Sprint 1/2 (no simplificaciones documentadas): `stageDurations` del Sandbox ahora se captura y loguea (antes descartado), nuevo test de redacción confirma que la URL firmada nunca se filtra en errores, `createOpenAiClient` centraliza timeout/retry para los tres clientes OpenAI, `OpenAiEmbeddingProvider` loguea `usage.total_tokens`. Se actualizaron los `tasks.md` transversales que quedaban desactualizados respecto al código ya implementado.

**Sprint 3 iniciado.** HU20 (historial de generaciones) implementado: `GET /project-versions/:id/test-runs?cursor&limit` → `Page<TestRunSummaryResponse>`, primera implementación real del patrón `Page<T>`/cursor del contrato (`INTEROP-1.1 → 1.2`, aditivo — pendiente sincronizar con las copias espejo de Developer Console/Sandbox, fuera de este workspace). `sddVersion` → `1.10`. Lint/test (167/167)/test:e2e (12/12)/build en verde; evidencia en `harness/reports/HU20-history.md`.

HU21/HU22 (progreso en tiempo real) también implementados: `RealtimeGateway` (`@nestjs/websockets` + Socket.IO, módulo `@Global()`) publica `project-version:update`/`test-run:update` por sala (`project-version:{id}`/`test-run:{id}`), reutilizando los mismos DTOs/mappers que ya usan los endpoints HTTP (`toProjectVersionResponse`/`toTestRunStatusResponse`, extraídos de sus servicios para compartirse). `IndexingJobHandler` y `TestGenerationJobHandler` emiten tras cada transición de estado persistida. Es un complemento del polling HTTP existente, nunca un reemplazo (`INTEROP-1.2 → 1.3`, aditivo — misma nota de sincronización pendiente con las copias espejo). Lint/test (174/174, incluye `realtime.gateway.spec.ts` nuevo)/test:e2e (12/12)/build en verde; evidencia y limitaciones (sin verificación e2e con `socket.io-client` real; sin auth por sala, consistente con el resto de endpoints V1) en `harness/reports/HU21-HU22-realtime-progress.md`.

Continúa: HU23 (autorreparación acotada: `RepairContext`/`RepairService`, `maxAttempts` configurable, loop generate→sandbox→failure→repair→rerun, nunca activo durante HU19 experimental); HU24 es Sprint 4, fuera de alcance por ahora.
