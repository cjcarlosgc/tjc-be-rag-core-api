# CHANGELOG

Todos los cambios notables de la línea base SDD se registran aquí. El contenido vigente vive en `spec/`; este archivo no reemplaza la especificación.

## [Unreleased]

- **SYSTEM-2.4 / INTEROP-2.4 (2026-09-20, `breaking: false`; contrato definido, pendiente de implementación) — HU58-HU64:** consolida `DEC-ORG-001` en el contrato. Nuevos `GET /workspaces` y `PATCH /projects/{projectId}`; `workspaceId` opcional en `POST /projects`, `GET /projects` y `GET /integrations/github/repositories`; `workspace` y `role` en `ProjectResponse`; `DELETE /projects/{projectId}` y la creación en organización solo para Admin; validaciones del binding (`REPOSITORY_OUTSIDE_WORKSPACE`, `REPOSITORY_PERMISSION_INSUFFICIENT`) y ocho códigos de error nuevos; matriz rol -> operación de todas las rutas (Reader/Maintainer/Admin); manejo de los eventos `member`, `membership`, `organization`, `team` y `repository` en el ingress y reconciliación horaria (ante una caída de GitHub se conserva lo existente y se niega lo nuevo); y login solo con GitHub con el `githubUserId` resuelto por la Admin API de Supabase. Es aditivo a nivel de cable; el retiro del login con correo y el rechazo de vínculos antes aceptados (repositorios ajenos) son cambios de comportamiento. Nueva feature `014-organizations-access` (5 cortes) y persistencia mínima en `spec/transversal/persistence`. Registra y aprueba `DEC-ORG-002` (2026-09-20, por el usuario) con cuatro casos borde que `DEC-ORG-001` no fijaba: el `read` implícito de un repositorio público no cuenta (en una organización se exige ser miembro activo); **los Projects personales no se comparten, solo se comparte mediante organizaciones** (enmienda la visibilidad de `DEC-ORG-001`, que decía "también en el workspace personal": un Project personal lo ve solo su creador como Admin y `project_access` existe solo para Projects de organización); un binding `REVOKED` deja el Project visible solo a los Admin; y `verify-app-access` y `branches` exigen permiso `maintain`/`write`/`admin` sobre el repositorio (corrección de seguridad del contrato anterior). Incluye la lista de decisiones derivadas, vetables por el usuario. Ciclo de corrección 1 de la revisión de contrato (2026-09-21): en una organización se exige SIEMPRE membresía activa además del permiso sobre el repositorio (un colaborador externo con `write` no accede); la corrección de seguridad del binding, `verify-app-access` y `branches` se separa como corte 4a y se publica primero (bundle A = identidad + 4a; bundle B = workspaces, roles y webhooks juntos), con la precondición de que la App no sea instalada por terceros antes; `breaking: false` queda cualificado con los cambios de comportamiento y el orden de despliegue (Console solo GitHub antes o a la vez que Core exija identidad GitHub); `GET /action-required?projectId` de un Project no visible pasa a `404`; se especifican default-deny, alta de acceso sin carreras, jobs de acceso robustos, revalidación de propietario y nombre del repositorio en la reconciliación, WebSocket con mapa socket -> Project y `SubscribeAck`, e identidad del bypass de desarrollo. Ciclo de corrección 2: `SubscribeAck` con tres resultados fijos y comportamiento del handshake (aditivo; Console emite `subscribe` sin callback y lo ignora); la Console envía `workspaceId` a `POST /projects` y `GET /projects` y consume `workspace`/`role` solo tras el sync de implementación del bundle B, y el bundle A espera a la Console solo-GitHub; dedupe de jobs de acceso con `dedupeKey` e índice único parcial solo sobre `PENDING`; advisory lock por `(projectId, userId)` como único mecanismo; predicado de acceso evaluado en petición (registro suficiente y binding no `REVOKED` o rol `ADMIN`); `POST .../enable` sobre `REVOKED` valida propietario y `repositoryId` (deja sin efecto para ese camino la simplificación de `T-002` registrada arriba); reconciliación del repositorio sobre todo Project con binding; una App desinstalada de la organización se distingue de un resultado no verificable (`404`, no `503`).

- **`DEC-ORG-001` APROBADO (2026-09-20) y HU58-HU64 aprobadas para implementación, solo especificación:** el spike contra GitHub real probó el permiso de colaborador con `Metadata: read` y la identidad `identities[].id` de la Admin API de Supabase; el rol de organización, la cobertura de eventos y los permisos nuevos de la App (`Members: read`, eventos `member`/`membership`/`organization`/`team`/`repository`) están documentados y se validan contra una organización real como condición de despliegue, con la App pública sin Marketplace. Consolida HU62 (login solo con GitHub) en la spec 012, `DEC-WEB-AUTH-001` y la misión. Detalle original (`PROPOSED`): uso organizacional. GitHub como fuente de verdad de la autorización, login solo con GitHub (HU62, cambio propuesto sobre la spec 012), workspaces personal y de organización, tres roles derivados de GitHub (Admin owner de la organización, Maintainer, Reader; redefine HU45), Projects creados por Admin dentro de una organización, binding acotado a la organización con permiso mínimo `maintain`/`write` y sin revincular, y revocación por webhook con reconciliación periódica, sin caché. Persistencia mínima: `userId -> githubUserId`, organización en `Project` y un registro de acceso. No se cambian SYSTEM-2.3/INTEROP-2.3, `sddVersion` ni `app/`.

- **HU56/HU57 implementado (INTEROP-2.3, `T-002-binding-lifecycle`):** HU57 en `repository-bindings/`: `POST .../integrations/github` resuelve el `repositoryId` real contra GitHub (un id distinto al enviado es `404 GITHUB_REPOSITORY_NOT_FOUND`), responde `409 REPOSITORY_ALREADY_BOUND` con mensaje genérico en el orden del contrato y traduce `P2002` a `409`; nueva ruta `POST .../enable` (idempotente, revalida la App, refresca `installationId`, reactiva también `REVOKED` si hay acceso); `disable` no degrada `REVOKED`; nueva columna `disabledReason` (`USER`|`INSTALLATION_SUSPENDED`) para que `installation.suspend` solo pause `ENABLED` y `unsuspend` solo rehabilite lo que la suspensión deshabilitó. HU56: `DELETE /projects/{id}` (`204`) con `projects.deletedAt` y una transacción única (marca, borra el binding, Runs no terminales -> `OBSOLETE`, publicaciones `PENDING`/`PUBLISHING` -> `FAILED`, jobs `PENDING` -> `FAILED`); `ownedProject()` filtra `deletedAt IS NULL` en todas las lecturas owner-scoped; los 4 job handlers usan `findForRun` (binding del mismo Project y Project vivo). Corrección tras revisión independiente (ciclo 1): el insert del binding toma `FOR SHARE` sobre el Project vivo en una transacción (evita un binding huérfano si un `DELETE /projects/{id}` corre entre las llamadas a GitHub y el insert), `findByRepositoryId` ignora bindings de Projects borrados (ingress de webhooks y conflicto del POST) y `get`/`disable`/`enable` responden `404 PROJECT_NOT_FOUND` antes de `REPOSITORY_BINDING_NOT_FOUND`. Simplificaciones: `enable` no compara el `repositoryId` real de GitHub con el persistido (un repo borrado y recreado con el mismo nombre tras `REVOKED` se reactivaría con un id viejo; sin contrato que lo pida); un `DISABLED` anterior a la migración (`disabledReason` null) se trata como pausa del usuario y `unsuspend` no lo reactiva; los jobs `PENDING` de Runs/publicaciones/experimentos se marcan `FAILED` con `PROJECT_DELETED` y los `RUNNING` no se interrumpen (quedan protegidos por `findForRun`); migraciones `20260920120000_binding_disabled_reason` y `20260920121000_project_soft_delete` aplicadas a Supabase el 2026-09-20 con `prisma migrate deploy` (`Database schema is up to date`). Evidencia: lint, 479/479 tests, `test:e2e` 8/8 y build en verde.

- **SYSTEM-2.3 / INTEROP-2.3 (2026-09-20, especificación aditiva; implementada) — HU56/HU57:** por `CONTRACT_SYNC` CS-20260920-001 (Console -> Core), probando el binding contra Core real (`500` al vincular un repositorio ya usado por otro Project). Nuevo `DELETE /projects/{projectId}` (`204`, borrado lógico sin restauración que libera el binding y oculta todo por API), nueva ruta `POST /projects/{projectId}/integrations/github/enable` (idempotente, revalida la App; reactiva también un binding `REVOKED` si la App recuperó acceso — decisión aprobada por el usuario), error `409 REPOSITORY_ALREADY_BOUND` con orden de validación y mensaje genérico, y reglas de que desconectar es pausa reversible (`DISABLED`) que nunca degrada `REVOKED` y que `installation.unsuspend` no reactiva lo pausado por el usuario. Work item `T-002-binding-lifecycle` (feature/T-002); el work item anterior se archivó en `harness/reports/T-002-analysis-run-domain.md`. Implementado en Core (ver entrada HU56/HU57 implementado). `sddVersion` se mantiene en 2.1; los mirrors de Console y Sandbox se sincronizan por `CONTRACT_SYNC`.

- **SYSTEM-2.2 / INTEROP-2.2 (2026-09-17, solo especificación):** HU29/HU30 sustituyen el onboarding installation-centric por repository discovery user-centric mediante provider token OAuth efímero y autorización/operación GitHub-App-centric. Se retiran como canónicas las rutas `installations` y `callback`; Core resuelve la instalación, valida la rama elegida y nunca acepta `installationId` desde el navegador. No se modifica `app/`.

- **HU30 implementado (§6.8, SYSTEM-2.2/INTEROP-2.2):** `GET /integrations/github/repositories` (discovery user-centric con `X-GitHub-Provider-Token`), `POST /integrations/github/repositories/verify-app-access`, `GET /integrations/github/repositories/{owner}/{repo}/branches`, `POST|GET|DELETE /projects/{projectId}/integrations/github`. `installationId` se resuelve server-side (`GithubAppAuthService.findInstallationForRepository`, JWT de App) y nunca se acepta del navegador; `integrationBranch` se valida contra las ramas reales del repositorio antes de persistir (columna sin default, migración `repository_binding_integration_branch_required`, pendiente de aplicar contra Supabase). `GithubAppAuthService`/`GithubRepositoryContentService` se movieron de `snapshot-intelligence/` a un módulo compartido `github-app/`. Nuevos códigos de error `GITHUB_ACCOUNT_REQUIRED`, `GITHUB_USER_TOKEN_INVALID`, `GITHUB_APP_ACCESS_REQUIRED`, `GITHUB_REPOSITORY_NOT_FOUND`, `INTEGRATION_BRANCH_NOT_FOUND`. `app.slug`/`app.displayName` de `verify-app-access` se resuelven dinámicamente contra `GET /app` (`GithubAppAuthService.getAppInfo()`, cacheado en memoria) en vez de env vars: no se llegaron a usar `GITHUB_APP_SLUG`/`GITHUB_APP_NAME` en ningún entorno y se eliminaron antes de documentarlas en Render. Smoke test end-to-end confirmado 2026-09-18 contra Render+Supabase+GitHub reales (verify-app-access/branches/create binding); discovery sigue sin probarse, requiere login GitHub OAuth real.

- **HU35/HU36 implementado (§6.11, incluye HU51):** `GET /action-required` (inbox cross-project), `GET /analysis-runs/{id}/context-questions`, `POST .../context-questions/{questionId}/answers`, `GET /projects/{id}/functional-knowledge`. `FunctionalContextEvaluatorService` (nuevo módulo `functional-knowledge/`) decide, tras HU33/34, si algún símbolo `DIRECTLY_CHANGED` `METHOD`/`FUNCTION` carece de `FunctionalKnowledge` `ACTIVE`; si es así crea una pregunta (una a la vez, adaptativa — sin total fijo) y el Run pasa a `ACTION_REQUIRED` (`AnalysisRunsService.markActionRequiredFromSystem`, nuevo). Responder resuelve conocimiento (`UNKNOWN` nunca crea regla), detecta conflicto contra una `ACTIVE` existente (`409 FUNCTIONAL_KNOWLEDGE_CONFLICT`, `conflictId` = `questionId`) y aplica `SUPERSEDE`/`KEEP_EXISTING`; cuando ya no faltan preguntas dispara `requestContinuation` (ACTION_REQUIRED->PROCESSING, ya existía sin wiring) y encola un job `functional-continuation` que reevalúa reutilizando los símbolos persistidos, sin re-materializar el snapshot. Simplificaciones documentadas: `normalizedRule` es la respuesta cruda (sin normalización semántica vía LLM), `visualAid` siempre `null`, conflicto por match exacto de scope+símbolo (sin jerarquía PROJECT⊃MODULE⊃CLASS). Modelos nuevos `FunctionalQuestion`/`FunctionalKnowledge` (migración `functional_knowledge`, aplicada contra Supabase). Nuevos códigos de error `FUNCTIONAL_QUESTION_NOT_FOUND`, `FUNCTIONAL_KNOWLEDGE_CONFLICT`.

- **Validation implementado (plan.md corte #6, sin HU propia — soporta HU32/HU39/HU40):** nuevo módulo `validation/`. Cuando un Run tiene contexto funcional suficiente (HU35/36), `AnalysisRunValidationJobHandler` genera (arm RAG, reutilizando `RetrievalService`/`ContextBuilder`/`PromptBuilder`/`LLMProvider` ya construidos para Experiments/HU19) y valida en Sandbox una prueba por cada símbolo `DIRECTLY_CHANGED` `METHOD`/`FUNCTION` sin test existente; clasifica el Run (`SUCCESS`/`BEHAVIORAL_MISMATCH`/`TECHNICAL_GENERATION_FAILURE`/`NO_ADDITIONAL_TESTS_REQUIRED`) y persiste `GeneratedTestProposal` (modelo nuevo, migración `generated_test_proposals` aplicada contra Supabase, contenido en Object Storage como `Artifact` ZIP-era). Cierra el hueco "Run queda en PROCESSING" que HU33-36 dejaron documentado explícitamente. Nuevo `GET /analysis-runs/{id}/test-proposals` (§6.12, parcial). `GithubSnapshotMaterializerService` ahora también incluye `pnpm-lock.yaml` (antes filtrado por `isPoolFile`) porque el Sandbox lo exige para instalar dependencias. Simplificaciones documentadas: sin baseline real (`phase=BASELINE`/`executionProfile` del contrato Sandbox 2.0 no existen en el `SandboxExecutionService` actual — subconjunto 1.6 vigente, `system-contract.md` §8 —, requiere migración coordinada con el repo del Sandbox; un símbolo con test existente se salta sin confirmar que siga en verde, sin `BASELINE_FAILED` todavía); `TEST_ASSERTION` se clasifica como `BEHAVIORAL_MISMATCH` y `COMPILATION`/`TEST_RUNTIME`/config como `TECHNICAL_GENERATION_FAILURE`, sin juicio semántico vía LLM para afinar esa distinción; solo arm RAG (`GENERALIST_AGENT` queda reservado para el experimento HU19); símbolos procesados en serie.

- **HU31 revocación implementado:** `github-webhooks.service.ts` ahora procesa `installation` (`deleted`->`REVOKED`, `suspend`->`DISABLED`, `unsuspend`->`ENABLED`, aplicado a todos los bindings de esa instalación vía `RepositoryBindingsRepository.updateStatusByInstallation`) e `installation_repositories` (`removed`->`REVOKED` solo el binding del repo puntual). `ENABLED` nunca reactiva un binding ya `REVOKED` (más fuerte que un `unsuspend` de la instalación completa). Sin dedup por delivery id para estos dos eventos: actualizar el status es naturalmente idempotente, no hace falta (`WebhookDelivery.prNumber`/`headSha` son obligatorios y estos eventos no los tienen). Cierra el único gap real que quedaba de HU31; el resto del ingress ya estaba implementado (`tasks.md` estaba desactualizado marcándolo `[ ]` por completo).

- **HU39 implementado (Checks nativos por HEAD):** nuevo módulo `checks/` (`AnalysisRunChecksService`) y `GithubChecksService` (`github-app/`, `POST /repos/{owner}/{repo}/check-runs`). Los 3 job handlers que completan un `AnalysisRun` (`snapshot-analysis-job.handler.ts`, `functional-continuation-job.handler.ts`, `analysis-run-validation-job.handler.ts`) publican un Check `completed` sobre `headSha` justo después de cada transición terminal/`ACTION_REQUIRED`, best-effort (un fallo publicando el Check no revierte ni oculta que el Run ya terminó en Core, solo se loguea). Mapeo `AnalysisRunStatus`->conclusión: `SUCCESS`/`NO_ADDITIONAL_TESTS_REQUIRED`/`NO_TEST_RELEVANT_CHANGES`->`success`; `BEHAVIORAL_MISMATCH`/`TECHNICAL_GENERATION_FAILURE`->`failure`; `BASELINE_FAILED`/`INFRASTRUCTURE_FAILURE`->`neutral` (`spec.md` caso 9: no le achaca el problema al PR); `ACTION_REQUIRED`->`action_required` (conclusión nativa de GitHub). `QUEUED`/`PROCESSING`/`OBSOLETE` no publican Check. `details_url` opcional vía `CONSOLE_BASE_URL` (nueva env var). Requiere que la GitHub App tenga `checks: write` habilitado en su configuración real (ya aprobado en `system-contract.md`, mínimo privilegio).

- **HU40 implementado (review, freshness y companion PR):** nuevo módulo `publications/` + `GithubGitDataService` (`github-app/`, Git Data API: blobs/trees/commits/refs + Pulls API). `POST /analysis-runs/{id}/test-publications` valida Run `SUCCESS`+vigente y `proposalIds` `AVAILABLE` de ese Run, crea `TestPublication` (`PENDING`) y encola `TestPublicationJobHandler`, que revalida freshness contra el HEAD real del PR (`GithubRepositoryContentService.getPullRequestHead`, nuevo) antes de escribir nada en GitHub -si cambió, `STALE`-; crea o reutiliza la rama `rag-tests/pr-<number>-<shortSha>` desde `run.headSha`, arma un commit con las propuestas (blob por archivo, tree sobre el tree base, commit con el HEAD validado como parent) y abre el companion PR hacia `run.headRef` (la feature branch, nunca `integrationBranch`). Nunca reabre un PR cerrado con ese head branch (`FAILED` explícito); reutiliza uno abierto si el job se reintenta. `GET /test-publications/{id}` expone el estado (`PENDING`/`PUBLISHING`/`PUBLISHED`/`STALE`/`FAILED`/`CLOSED`). Modelo nuevo `TestPublication` (migración `test_publications`, `proposalIds String[]` sin tabla de join separada). Nuevos códigos de error (el contrato no los listaba explícitamente): `TEST_PUBLICATION_NOT_FOUND`, `TEST_PUBLICATION_INVALID_RUN_STATUS`, `TEST_PUBLICATION_PROPOSAL_NOT_AVAILABLE`. Con esto, §6.12 queda completo. Requiere `contents`/`pull requests: write` habilitado en la GitHub App real (ya aprobado como mínimo privilegio en `system-contract.md`, "solo al habilitar publicación").

- **Contrato (INTEROP-2.1/SYSTEM-2.1, aditivo, definido sin implementar) — HU48/HU51/HU53/HU55:** Console formalizó estas 4 historias en su backlog (2026-09-14, `PROPOSED`) señalando que estaban bloqueadas por falta de contrato de Core; se espejan en `spec/backlog.md` y se define su contrato HTTP: `CreateExperimentRequest` (§6.5) reapunta la unidad experimental de `TestTarget` (id interno, ZIP-era) a `analysisRunId` + identidad de símbolo (`filePath`/`qualifiedName`, igual que `AnalysisSymbolResponse`), con `404 ANALYSIS_SYMBOL_NOT_FOUND`/`422 UNSUPPORTED_SYMBOL_KIND` si el símbolo no es `DIRECTLY_CHANGED` o no es `METHOD`/`FUNCTION`; `AnalysisRunDetailResponse.history` (§6.10) expone el historial append-only de transiciones de status (`AnalysisRunTransitionResponse`, 8 motivos derivados 1:1 de los call sites reales de `AnalysisRunsService.transitionTo`); `GET /analysis-runs` sin `projectId` (§6.10) lista Runs de todos los Projects del usuario autenticado; `SubmitFunctionalAnswerRequest.conflictResolution` + `409 FUNCTIONAL_KNOWLEDGE_CONFLICT` (§6.11) exponen cuándo una respuesta contradice una `FunctionalKnowledge` `ACTIVE` existente, dejando la decisión (`SUPERSEDE`/`KEEP_EXISTING`) al usuario en vez de resolverla automáticamente. Cada bloque queda marcado **"Definido, pendiente de implementación"**: no se toca código de Core en este corte (ni el `CreateExperimentDto`/controller reales, que siguen con la forma `projectId`/`targetId` hasta que se implemente HU48). No amerita bump de versión documental: es aditivo sobre `INTEROP-2.1`/`SYSTEM-2.1` ya vigente, mismo patrón que otras adiciones `INTEROP-1.x → 1.y (aditivo)` de este changelog.

- **SDD 2.1 / SYSTEM-2.1 / INTEROP-2.1 (bump de versión, decisión explícita del usuario):** tras registrar el retiro de ZIP/generación manual (bullet siguiente) inicialmente como corrección sin bump, el usuario pidió explícitamente versionar el cambio como `2.1` y homologarlo en los tres repositorios. Se actualizan los headers de `system-contract.md`/`interoperability-contract.md` (`SYSTEM-2.0`→`SYSTEM-2.1`, `INTEROP-2.0`→`INTEROP-2.1`), `harness/state.json` (`sddVersion: "2.1"`) y toda referencia declarativa de línea base vigente en `spec/` (`README.md`, `backlog.md`, `constitution/mission.md`, `constitution/project-context.md`, y las specs/plans/tasks de features/transversal que citaban `SYSTEM-2.0`/`INTEROP-2.0` como su contrato). No se reescribe `spec/backlog-migration-sdd-2.0.md` (reporte histórico de T-001) ni menciones puramente históricas de "SDD 2.0" (p. ej. "SUPERSEDED BY SDD 2.0 / T-001", títulos de documento). Se envía handoff a las sesiones de Developer Console y Test Execution Sandbox para que homologuen sus propias copias de SDD/contratos a `2.1`.
- **Retiro de ZIP upload y generación manual como producto (SYSTEM-2.1/INTEROP-2.1):** se retira, como ruta de producto vigente, la carga manual de proyecto vía ZIP (`POST /projects/index`) y los 5 modos manuales de generación (`TARGET|CLASS_ALL|CLASS_MISSING|PROJECT_MISSING|PROJECT_ALL`, HU08-HU12), junto con `POST /test-runs` y endpoints asociados (estado, resultados, historial HU20, retry manual HU24, artefactos HU15-HU18) y el evento realtime `test-run:update` (HU22). El único disparador de análisis real pasa a ser PR-driven (`AnalysisRun`, HU30-HU32 ya implementado). Esto endurece la clasificación `ADAPT` que `spec/backlog-migration-sdd-2.0.md` había registrado para ZIP/modos manuales en el re-baseline de T-001 (reporte histórico, no se reescribe); `spec/backlog.md` y los `spec.md` de `002/003/005/007/009` quedan como la fuente vigente. `system-contract.md` ("Compatibilidad y legado") e `interoperability-contract.md` (§6.2-6.4, §6.6-6.7, §8) se corrigen en consecuencia.
  - **Experiments (HU19, RAG vs GENERALIST_AGENT) se conserva** — decisión explícita, no alcanzada por este retiro. Referencia: handoff de reorientación P0/P1/P4 provisto por el usuario (unidad experimental futura basada en `AnalysisRun` vía "Run comparison"/"Capture next PR"; ese rediseño es un corte P1/P4 posterior, no bloquea el desarrollo P0 en curso). Mientras tanto, `POST /experiments` (`CreateExperimentRequest.targetId`) queda sin una ruta vigente para crear targets nuevos porque dependía de la indexación ZIP retirada; el código y el DTO no se tocan en este corte.
  - Sin cambios de esquema Prisma ni migraciones: los modelos `TestGenerationRun`/`TargetRunResult`/`Artifact`/`GenerationMode`/`TestRunStatus`/`TargetRunStatus` quedan en `schema.prisma` sin uso desde la aplicación, para no perder datos históricos. `TestTarget`/`ProjectVersion`/`CodeChunk` se conservan: los reutiliza HU33/34 (snapshot intelligence, adaptando el origen de `ProjectVersion` a commit SHA) y `ExperimentRun` todavía los referencia.

- **SDD 2.0 / SYSTEM-2.0 / INTEROP-2.0 (T-001):** se reemplaza la dirección manual/import-driven por una arquitectura PR-driven con GitHub App dentro de Core, repository binding por Project, `AnalysisRun` ligado a PR/HEAD, jobs/attempts separados, trigger positivo hacia `integrationBranch` configurable y Checks con freshness. GitHub OAuth queda limitado al login mediante Supabase Auth; no habilita repositorios ni se agrega Google OAuth.
- `CHANGESET` pasa a ser la unidad de validación y se separa de `INDEX DELTA`; el Context Builder objetivo combina contexto semántico, estructural, funcional y tests existentes. Se aprueban Functional Knowledge versionado, `ACTION_REQUIRED`, continuation sobre el mismo Run/HEAD, Focus Mode y Action Required inbox.
- Se formalizan baseline-first y las conclusiones `BEHAVIORAL_MISMATCH`, `TECHNICAL_GENERATION_FAILURE`, `INFRASTRUCTURE_FAILURE`, `BASELINE_FAILED`, `NO_ADDITIONAL_TESTS_REQUIRED`, `NO_TEST_RELEVANT_CHANGES` y `OBSOLETE`. La publicación de tests requiere revisión humana, freshness y companion PR hacia la feature branch, sin auto-merge ni recursión.
- TypeScript/Jest/Vitest queda en compatibility track y PHP/Laravel/PHPUnit pasa a active development track mediante adapters en Core y execution profile real en Sandbox. El experimento `RAG` vs `GENERALIST_AGENT` permanece.
- Se agrega backlog HU30-HU47, mapa KEEP/ADAPT/DEFER/DROP y roadmaps diferenciados: Console mock-first; Core/Sandbox real-implementation-first. HU26 GitHub login/import queda superseded; ZIP y generación manual permanecen como legacy/development mientras sean útiles.
- `DEC-GH-001` pasa a APROBADO. `DEC-MET-001`, `DEC-INF-001`, `DEC-VAL-001` y nueva `DEC-EXP-FK-001` permanecen PENDING con blocks acotados. T-001 no implementa GitHub/PHP productivos ni inicia automáticamente el roadmap posterior.

- **SDD 1.16 / SYSTEM-1.6 / INTEROP-1.6 (consolidación 2026-09-11, solo especificación):** se incorporan en Sprint 4 `HU27` (explorador de contexto RAG), `HU28` (contexto observado por el agente generalista) y `HU29` (Supabase Auth por correo y autorización por propietario para el flujo ZIP). Se aprueban trazas por target/intento con candidatos seleccionados/descartados, razones observables, tool calls normalizadas, hashes, rangos, snippets acotados y reconstrucción de tres líneas circundantes desde la `ProjectVersion` inmutable; `ArtifactResponse` agrega `targetIds` y `INTEROP-1.6` publica listados/detalle de trazas. `DEC-WEB-AUTH-001` queda `APROBADO`; `DEC-GH-001` conserva la integración real de login/repositorios/ramas/PR como futuro `PENDING`, pero no bloquea una demo local mock claramente rotulada y sin efectos externos dentro de HU26. `DEC-VAL-001` continúa pendiente para las condiciones empresariales restantes. No se modifica `app/`, Supabase ni infraestructura.

- **Configuración de despliegue local + `LLM_REASONING_EFFORT`:** se configuran las credenciales reales de OpenAI y el Sandbox local (`SANDBOX_URL=http://localhost:3001`) en el `.env` de desarrollo (no versionado). `OpenAiLLMProvider` ahora soporta `LLM_REASONING_EFFORT` (opcional; `none|minimal|low|medium|high|xhigh|max`) — se incluye `reasoning_effort` en el request a `chat.completions.create` solo cuando está configurado, sin afectar modelos que no lo soportan. `EMBEDDING_MODEL` se mantiene `text-embedding-3-small` (`DEC-EMB-001`, sin cambios). No es una decisión de arquitectura ni toca contrato/DTOs, es config operativa del adapter OpenAI ya abstraído por `LLMProvider`.

- **SDD 1.15 / SYSTEM-1.5 (cierre de implementación, sin cambios de contrato):** se implementa en `app/` lo que `SDD 1.14` había aprobado solo como especificación, más un fix de CORS pedido directamente por el usuario al preparar la integración con Sandbox/Frontend reales:
  - **CORS:** `main.ts` llama a `app.enableCors()`; sin esto, ningún request HTTP del frontend (en otro origen) pasaba el preflight del navegador.
  - **`DEC-AUTH-001`:** `SANDBOX_SERVICE_TOKEN` (nueva variable, opcional pero exigida junto con `SANDBOX_URL` — la app falla al arrancar ante una configuración parcial de ambas). `SandboxExecutionService` envía `Authorization: Bearer <token>` en las tres llamadas a `/executions` (POST, y los dos GET de status/resultado).
  - **`DEC-IDEMP-001`:** nuevo modelo `IdempotencyRecord` (migración `20260907010601_idempotency_records`, aplicada contra la Supabase real) con `scope`/`idempotencyKey`/`requestFingerprint`/`operationId`/`createdAt` y unicidad compuesta `(scope, idempotencyKey)`, exactamente como especifica `spec/transversal/persistence/spec.md`. `IdempotencyService` centraliza: validación del header (`400 IDEMPOTENCY_KEY_REQUIRED`/`INVALID_IDEMPOTENCY_KEY`), huella canónica (`canonicalJsonStringify`, keys JSON ordenadas recursivamente), reserva+creación+encolado atómicos en una transacción, y resolución de conflicto/replay/carrera concurrente (`409 IDEMPOTENCY_CONFLICT` o reconstrucción de la respuesta 202 original vía `operationId`, sin persistir el cuerpo de la respuesta). Aplicado a `POST /test-runs`, `POST /experiments` y `POST /test-runs/{runId}/targets/{targetId}/retry`. Para el fan-out Core→Sandbox, `sandbox-request-id.util.ts` deriva UUID v5 estables (namespace `6ba7b811-9dad-11d1-80b4-00c04fd430c8`) a partir del `jobId` (ahora recibido por los 3 job handlers vía el segundo parámetro de `JobHandler.handle`), reemplazando las keys aleatorias anteriores.
  - No cambia ningún DTO, ruta o tipo ya documentado en `interoperability-contract.md`/`system-contract.md` — estos ya habían aprobado todo esto en `SDD 1.14`; esta entrada cierra la brecha de implementación, ver `spec/transversal/persistence/spec.md` y `spec/transversal/async-jobs/spec.md`.
- **SDD 1.14 / SYSTEM-1.4 / INTEROP-1.5 (solo especificación):** `DEC-AUTH-001` aprueba Bearer con un secreto opaco precompartido `SANDBOX_SERVICE_TOKEN`; `DEC-IDEMP-001` fija keys durables en los POST de generación/experimento/retry e identidades hijas UUID v5 por subejecución Sandbox. Se incorpora la consecuencia compartida de `DEC-SBX-002`: Sandbox V1 ejecuta únicamente proyectos con `pnpm-lock.yaml`. Core y Sandbox existen por separado, pero la integración real sigue pendiente hasta implementar Bearer e idempotencia estable en Core. No se modificó código, `.env`, migraciones, base de datos ni infraestructura.
- **HU25 (listado de proyectos y versiones, `010-product-experience`):** se implementa `GET /projects?cursor&limit` → `Page<ProjectResponse>` y `GET /projects/{projectId}/versions?cursor&limit` → `Page<ProjectVersionSummaryResponse>`. No es un cambio de contrato: ambas rutas y sus tipos ya estaban definidos en `interoperability-contract.md` desde `INTEROP-1.1` (sección 8: "listado de proyectos y versiones quedan aprobados para implementar"), pero nunca se habían construido. `current: true` marca únicamente la versión cuyo id coincide con `Project.currentVersionId`. Mismo patrón `Page<T>`/cursor (`take + 1`) ya usado en HU20. HU26 (experiencia visual consolidada) no tiene alcance de backend — ver `spec/features/010-product-experience/spec.md`.
- **INTEROP-1.3 → 1.4 (aditivo):** se agrega `POST /test-runs/{runId}/targets/{targetId}/retry` → `202 TargetRetryAcceptedResponse` para HU24 (reintento manual, cierra Sprint 3 completo — HU20-HU22 + HU24, HU23 descartada). Reintenta desde cero, a pedido explícito del usuario, un target puntual que quedó `INVALID`/`FAILED` en un run ya terminado, reutilizando exactamente el mismo pipeline de un-solo-target (retrieval → prompt → LLM → merge → Sandbox) que el primer intento — sin ningún mecanismo de corrección automática de por medio (consistente con la decisión de HU23 documentada abajo). No crea un run nuevo ni toca otros targets del mismo run: `RetryTargetJobHandler` actualiza en su lugar el `TargetRunResultResponse` y el `Artifact` existentes (nunca los duplica) y reajusta `validTargets`/`invalidTargets`/`failedTargets`/`status` del run (`TestGenerationRunsRepository.applyRetryOutcome`). Solo se puede reintentar un target en estado `INVALID`/`FAILED` (`409 TARGET_RETRY_NOT_ALLOWED` en cualquier otro caso) y solo si el run ya llegó a un estado terminal (`409 TEST_RUN_NOT_FINISHED`). No cambia ningún DTO ni ruta existente. Pendiente: sincronizar esta adición en las copias espejo de Developer Console y Test Execution Sandbox (fuera de este repositorio).
- **HU23 (implementado y revertido en la misma sesión; decisión definitiva de arquitectura):** se implementó una autorreparación acotada (`RepairService`/`RepairContext`, `GENERATION_MAX_REPAIR_ATTEMPTS`, `TargetRunResult.repairAttempts`) y se revirtió por completo a pedido explícito: **una generación validada equivale a una única ejecución en el Sandbox.** Si falla por compilación, ejecución o assertions, el resultado queda `INVALID`/`FAILED` tal cual — sin ningún reintento automático de corrección vía LLM. Esta no es una puerta `PENDING` ni queda abierta como evolución futura: es alcance definitivo de la arquitectura. Se revierte todo el código agregado (`src/generation/repair/`, el loop en `TestGenerationJobHandler`, la config), se elimina `TargetRunResult.repairAttempts` (migración de baja aplicada contra Supabase, historial de migraciones conserva el par alta+baja para que un despliegue desde cero siga siendo reproducible) y se retira `GENERATION_MAX_REPAIR_ATTEMPTS`. `spec/backlog.md` marca HU23 como descartada; HU24 (Sprint 4) queda redefinida como un reintento manual explícito desde cero, sin depender de ningún intento automático previo (nunca existieron).
- **INTEROP-1.2 → 1.3 (aditivo):** se agrega `RealtimeGateway` (WebSockets vía `@nestjs/websockets`/Socket.IO) para HU21/HU22 (progreso de indexación y de generación de pruebas en tiempo real). Suscripción por sala e id explícito (`subscribe:project-version`/`subscribe:test-run`, salas `project-version:{id}`/`test-run:{id}`); el servidor nunca hace broadcast global. Eventos `project-version:update`/`test-run:update` reutilizan los DTOs HTTP existentes (`ProjectVersionResponse`/`TestRunStatusResponse`, mapeados con las funciones ya extraídas `toProjectVersionResponse`/`toTestRunStatusResponse`) en vez de definir formas nuevas. `IndexingJobHandler` y `TestGenerationJobHandler` emiten tras cada transición de estado persistida. Es un complemento del polling HTTP existente, nunca un reemplazo — ambos endpoints de estado siguen funcionando igual. No cambia ningún DTO ni ruta HTTP existente. Pendiente: sincronizar esta adición en las copias espejo de Developer Console y Test Execution Sandbox (fuera de este repositorio).
- **INTEROP-1.1 → 1.2 (aditivo):** se agrega `GET /project-versions/{projectVersionId}/test-runs?cursor&limit` → `Page<TestRunSummaryResponse>` para HU20 (historial de generaciones), primera implementación real del patrón `Page<T>`/cursor ya descrito en el contrato desde `INTEROP-1.0` pero nunca antes implementado en código. No cambia ningún DTO ni ruta existente. Pendiente: sincronizar esta adición en las copias espejo de Developer Console y Test Execution Sandbox (fuera de este repositorio).

- Se resuelven pendientes reales detectados al revisar Sprint 1/2 tras cerrar HU19 (no simplificaciones documentadas, gaps genuinos): `SandboxExecutionResult` ahora captura `stageDurations` (antes descartado; se loguea la duración por etapa del Sandbox); nuevo test de redacción confirma que la URL firmada del `EphemeralDownloadRef` nunca aparece en un mensaje de error; `createOpenAiClient` centraliza timeout/retry (`OPENAI_TIMEOUT_MS`/`OPENAI_MAX_RETRIES`) para `OpenAiEmbeddingProvider`/`OpenAiLLMProvider`/`GeneralistAgentService`; `OpenAiEmbeddingProvider` ahora loguea `usage.total_tokens` por batch. Se actualizan además los `tasks.md` de `persistence`/`object-storage`/`providers`/`observability`/`experimental-metrics` para reflejar lo ya implementado en 004-008 (varios quedaban marcados `[ ]` aunque el código ya los cubría).
- Se implementa `008-experimental-comparison` (HU19), cierre de Sprint 2: `POST /experiments` ejecuta 3 repeticiones RAG + 3 del agente generalista (`GENERALIST_AGENT`, `DEC-EXP-002`) por target, cada una en un workspace fresco. El agente explora el snapshot con 4 herramientas read-only (`WorkspaceAgentTools`: listar, leer, buscar texto, inspeccionar símbolo vía ts-morph) mediante un loop de tool-calling real de OpenAI (`GeneralistAgentService`), sin recibir el código del target directamente (a diferencia de RAG); ambos brazos comparten timeout de generación y validación en el Sandbox. `GET /experiments/:id/results` agrega tasas, medias de tiempos/tokens/costo y distribución de `failureType` por estrategia. Ver `harness/reports/008-experimental-comparison.md` (limitaciones: sin Sandbox ni OpenAI reales disponibles en este workspace; `inspect_symbol` combina 3 sub-capacidades de TS language service como simplificación documentada).
- Se implementa el pipeline de generación de pruebas (`005-test-generation` + `006-validation-orchestration` + `007-artifacts`, HU08-HU18): `POST /test-runs` resuelve targets según el modo (`GapAnalyzer`), recupera contexto (004) y genera con `LLMProvider`/`OpenAiLLMProvider`, aplica CREATE/MERGE seguro vía ts-morph (`TestFileMergeService`, preserva pruebas existentes), valida cada target contra el Test Execution Sandbox (`SandboxExecutionService`, contrato `INTEROP-1.1` completo con URLs firmadas efímeras) y persiste/entrega artefactos (`ArtifactService`: upload, diff LCS, download individual/ZIP). Endpoints: `GET /test-runs/:id`, `/results`, `/artifacts`, `GET /artifacts/:id/diff|download`. Ver `harness/reports/005-006-007-generation-pipeline.md` para el detalle y las limitaciones documentadas (sin Sandbox real disponible en este workspace; `evidenceIds` y validación batch final quedan pendientes).
- Se implementa el rework de chunking de `002-project-version-indexing` conforme a `DEC-CHUNK-001` (jerárquico + oversized structured chunks + `importsUsed` + `tokenCount` real vía `gpt-tokenizer`) y `004-rag-retrieval-context` (`RetrievalService` con señal estructural bidireccional por imports + vector query pgvector cosine, `ContextBuilder` con score/dedupe/budget configurables). Ver `harness/reports/004-rag-retrieval-context.md`.
- **SDD 1.9:** se resuelven `DEC-EMB-001`, `DEC-CHUNK-001` y `DEC-EXP-002` (todas `APROBADO`). `DEC-EMB-001`: `text-embedding-3-small`/1536 dimensiones queda definitivo; `voyage-code-4` sigue como candidato futuro. `DEC-CHUNK-001`: chunking jerárquico (clase completa + chunks hijos `METHOD`/`CONSTRUCTOR` con `parentSymbolName`), "oversized structured chunks" divididos por `maxChunkTokens` configurable sin overlap textual pero con identidad/orden/adyacencia para expansión dinámica en 004, metadata `importsUsed`, `tokenCount` real vía tokenizer, e identidad de chunk scoped a la `ProjectVersion` sin continuidad entre versiones. `DEC-EXP-002`: contrato operativo del `GENERALIST_AGENT` — herramientas read-only ampliadas (incluye TS language service), snapshot vía el mecanismo de materialización existente, trayectoria completa persistida, exclusión de tests existentes del target, límites/paridad simétricos con RAG. Ninguna resolución modifica código en `app/`; el rework de chunking sobre Sprint 1 y la implementación de 004/008 quedan como trabajo futuro. Se aclara además `DEC-INF-001` (sigue `PENDING`): Docker Desktop local confirmado suficiente para Sprint 2-4, selección de proveedor remoto se revisita tras Sprint 4, sin fallback automático si un futuro proveedor gratuito falla. `DEC-VAL-001`, `DEC-MET-001` y `DEC-RAG-001` no se tocan.
- **SDD 1.8 / SYSTEM-1.3 / INTEROP-1.1:** se consolida a RAG Core como único propietario de Supabase Storage, PostgreSQL/pgvector, jobs y resultados. Core entrega al Sandbox URLs firmadas efímeras verificables; Sandbox no recibe credenciales Supabase/DB y Frontend consume solo la API. Se registra el proyecto Supabase, bucket privado `repository-zips`, configuración canónica, `upsert=false`, key versionada y migraciones Prisma como fuente del esquema. No se aplicaron migraciones remotas ni se cerraron decisiones no relacionadas.
- **SDD 1.7 / SYSTEM-1.2:** se registra la MacBook encendida con Docker Desktop y su VM Linux como entorno temporal aprobado para desarrollo/prevalidación del Sandbox. El destino previsto es una VM Linux remota; `DEC-INF-001` mantiene PENDING la selección de un proveedor preferentemente gratuito y bloquea solo su aprovisionamiento remoto. No se decide exposición temporal, imagen Node, package managers ni condiciones de validación empresarial. INTEROP-1.0 no cambia.
- **SDD 1.6:** se consolida una línea base SDD homologada para los tres repositorios y se formaliza la política Git común: commits por cambio coherente con trazabilidad obligatoria `Refs: HU...`, revisión por work item y revisión consolidada documentada del sprint antes del push. `sddVersion` deja de tratarse como versión local independiente; `SYSTEM-*` e `INTEROP-*` conservan versionado propio. Un push anticipado exige la misma puerta y commit/push siguen requiriendo solicitud humana explícita.
- **Ajuste de revisión SDD 1.6:** se permite que un commit exclusivo `docs(review)` incorpore la evidencia después del veredicto, sujeto a una comprobación final de que no contiene otros cambios. Se corrige además el diagnóstico de `decisionGate` malformado y la validación de espacios finales.
- **SDD 1.5 / SYSTEM-1.1 / INTEROP-1.0:** se aprueba el contrato universal de interoperabilidad para los tres componentes. Conserva las rutas Sprint 1, fija DTOs de listado/generación/validación/artefactos/experimento y resuelve Core↔Sandbox mediante `202 + polling`, idempotencia, correlación, autenticación interna y referencias verificables de Object Storage. `DEC-INT-001` pasa a APROBADO; las demás puertas conservan su alcance.
- **SDD 1.4 / SYSTEM-1.0:** se crea el contrato canónico de los tres componentes, con RAG Core como propietario y copias espejo en frontend/Sandbox. Se unifican topología, TypeScript-only, Supabase Storage + `ObjectStorageService`, experimento `RAG|GENERALIST_AGENT`, validación empresarial y estados de disponibilidad. `DEC-INT-001` bloquea únicamente la integración HTTP Core↔Sandbox hasta definir su DTO/transporte. HU06/HU08 se corrigen para incluir funciones.
- **Cambio de contrato aprobado (SDD 1.2 → 1.3):** el alcance implementable V1 queda exclusivamente en TypeScript (`.ts/.tsx`), aunque académicamente se ubique en el ecosistema JavaScript/TypeScript. El baseline aislado queda sustituido por `GENERALIST_AGENT`, un agente contemporáneo con exploración controlada y trazable del mismo snapshot; HU19 compara esa estrategia contra la arquitectura RAG especializada. La validación final se orienta al área de desarrollo de una empresa real, sin trasladar al SDD datos académicos o administrativos. Se documenta la topología de dos backends y un frontend.
- SDD 1.3 incorpora puertas de decisión acotadas: `DEC-CHUNK-001` y `DEC-EMB-001` bloquean el retrieval definitivo; `text-embedding-3-small` continúa como default provisional y `voyage-code-4` como candidato, sin congelar dimensionalidad final; `DEC-EXP-002` bloquea HU19 hasta definir las herramientas/paridad del agente generalista. `DEC-RAG-001` (posible señal test-aware) y `DEC-MET-001` (mutation score/StrykerJS) quedan PENDING para investigación posterior al núcleo de Sprint 2 y no bloquean trabajo no relacionado. Se descartan la prohibición general por leakage y la ablación obligatoria semántico/estructural/híbrido.
- Se explicita en `002-project-version-indexing` el diseño realmente implementado: discovery filtrado, parsing ts-morph, chunk por declaración top-level con fallback `FILE`, inventario separado, embeddings batch, persistencia pgvector y responsabilidades de los servicios. No se modifica código ni se declara definitiva esa estrategia para retrieval.
- Se incorpora contexto operativo canónico y una puerta de decisiones por work item en el harness. El cambio separa los handoffs de tesis del contrato de software, evita bloqueos por pendientes no relacionados y documenta vacíos de completitud; no altera comportamiento funcional ni incorpora contenido académico.
- Se resuelve la decisión PENDING de package manager: `pnpm` (`spec/constitution/tech-stack.md`).
- Se implementa HU01 (`001-project-management`): scaffold NestJS + Prisma/PostgreSQL en `app/`, módulo `projects` (crear/consultar), envelope de error estándar, correlationId, config validada y health check.
- Se resuelve la decisión PENDING de mecanismo durable de jobs: cola DB-backed en PostgreSQL (`spec/constitution/architecture.md`, `spec/transversal/async-jobs/`).
- Se resolvió inicialmente la decisión PENDING de proveedor de Object Storage con un adaptador S3-compatible; esta decisión queda sustituida por el cambio SDD 1.2 descrito a continuación.
- Se implementan HU02, HU03, HU04, HU05 y HU07 (`002-project-version-indexing`): carga de ZIP con validación síncrona (formato/tamaño/compatibilidad), indexación asíncrona vía cola de jobs DB-backed (extracción segura, parsing ts-morph, chunking, embeddings OpenAI, persistencia en pgvector), y endpoints de progreso/resultados.
- **Cambio de contrato aprobado (SDD 1.0 → 1.1):** `TestTarget.targetType` pasa de `CLASS|METHOD` a `CLASS|METHOD|FUNCTION` para permitir funciones top-level como objetivos testables sin forzarlas a la forma `METHOD` sin clase (`004-rag-retrieval-context`). Reglas: CLASS usa `symbolName` sin `methodName`; METHOD usa `symbolName` (clase) + `methodName`; FUNCTION usa `symbolName` (función) sin `methodName`. Como consecuencia, el modo de generación puntual de `005-test-generation` se renombra de `METHOD` a `TARGET` (resuelve un `TestTarget` de tipo METHOD o FUNCTION indistintamente). Se ajusta también `003-test-inventory` para referenciar el mismo contrato.
- Se implementa HU06 (`003-test-inventory`): inventario de `TestTarget` (clases/métodos/funciones exportados), detección de framework Jest/Vitest sin inventar cuando es ambiguo, resolución heurística de pruebas existentes por convención de imports, y exposición vía `GET /project-versions/:id/results` (resumen) y `GET /project-versions/:id/test-inventory` (detalle).
- **Cambio de decisión aprobado (SDD 1.1 → 1.2):** el proveedor concreto de Object Storage pasa a ser Supabase Storage vía `@supabase/supabase-js`, encapsulado detrás de la abstracción interna `ObjectStorageService`. PostgreSQL + pgvector en Supabase permanece como almacén de datos de dominio, chunks, embeddings vectoriales y cola DB-backed de jobs (`spec/constitution/tech-stack.md`, `spec/constitution/architecture.md`, `spec/transversal/object-storage/`).
- Se implementa la migración de código del cambio SDD 1.2: se retira el adaptador S3-compatible (MinIO) y sus dependencias (`@aws-sdk/*`), se agrega `SupabaseObjectStorageAdapter` (`@supabase/supabase-js`) detrás de `ObjectStorageService` (clase abstracta, sirve como su propio token de inyección), con verificación/creación de bucket perezosa para no acoplar el arranque de la app a la disponibilidad de Supabase. Ver `harness/reports/object-storage-supabase-migration.md`.
- Fix: `ProjectVersionsService.startIndexing` marca la `ProjectVersion` como `FAILED` (en vez de dejarla huérfana en `PENDING`) si falla la subida del snapshot, `setSnapshot` o el encolado del job; evita que un `Project` quede bloqueado permanentemente con 409 `PROJECT_INDEXING_IN_PROGRESS` tras un fallo transitorio de storage/jobs.

## [1.0.0] - 2026-08-30

- Se crea la línea base SDD del proyecto.
- Se adopta `spec.md + plan.md + tasks.md` por feature.
- Se adopta `CHANGELOG.md` en lugar de enmiendas acumulativas dentro de las specs.
- Se conserva trazabilidad mediante `storyIds` y `sprint`.
- El código fuente se reserva para `app/`.
